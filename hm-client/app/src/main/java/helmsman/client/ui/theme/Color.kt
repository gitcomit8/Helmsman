package helmsman.client.ui.theme

import androidx.compose.ui.graphics.Color

object DeathStrandingColors {
    // Backgrounds — deep space navy, barely-lit surfaces
    val DeepNavy       = Color(0xFF060912)
    val DarkNavy       = Color(0xFF090D1C)
    val MidNavy        = Color(0xFF0D1228)
    val Navy           = Color(0xFF131930)
    val LightNavy      = Color(0xFF1A2240)
    val SurfaceCard    = Color(0xFF0C1020)
    val SurfaceElevated= Color(0xFF111628)
    val Border         = Color(0xFF252D50)
    val BorderGlow     = Color(0xFF303C70)

    // Gold Palette — warm, rich, unmistakeable
    val Gold       = Color(0xFFFFD700)
    val GoldBright = Color(0xFFFFE566)
    val Amber      = Color(0xFFFFAA00)
    val DarkGold   = Color(0xFFBB8800)
    val PaleGold   = Color(0xFFFFF0AA)
    val BurntGold  = Color(0xFFCC7700)
    val GoldMuted  = Color(0xFF7A5C00)

    // Text — properly readable on dark navy backgrounds
    val TextPrimary   = Color(0xFFEEEAE0)   // warm white, near-white
    val TextSecondary = Color(0xFFB0A890)   // warm light gray, good contrast
    val TextMuted     = Color(0xFF7A7060)   // dark gold-gray, visible but subtle

    // Status / semantic
    val ErrorRed     = Color(0xFFFF4444)
    val SuccessGreen = Color(0xFF3DDA8A)
    val WarningAmber = Color(0xFFFFAA40)
    val InfoCyan     = Color(0xFF2AD5D5)
    val StreamBlue   = Color(0xFF4DA8FF)
}
